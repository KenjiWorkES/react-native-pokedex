import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    alignItems: 'center',
  },
  imageContainer: {
    marginTop: 50,
    alignItems: 'center',
    marginBottom: 30,
    width: 150,
    height: 150,
  },
  image: {
    resizeMode: 'contain',
    width: '100%',
    height: '100%',
  },
});
