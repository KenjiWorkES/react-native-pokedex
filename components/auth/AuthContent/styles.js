import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  imageContainer: {
    marginTop: 70,
    width: '100%',
    alignItems: 'center',
  },
  image: {
    width: '50%',
  },
});
